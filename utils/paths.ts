import { join } from 'path'

export const PLUGIN_ROOT_PATH = join(__dirname, '../')
export const TEMPLATES_PATH = join(PLUGIN_ROOT_PATH, './templates')
export const BASE_TEMPLATE_PATH = join(TEMPLATES_PATH, './base')
